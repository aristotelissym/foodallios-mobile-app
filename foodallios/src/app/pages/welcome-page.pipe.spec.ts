import { WelcomePagePipe } from './welcome-page.pipe';

describe('WelcomePagePipe', () => {
  it('create an instance', () => {
    const pipe = new WelcomePagePipe();
    expect(pipe).toBeTruthy();
  });
});
